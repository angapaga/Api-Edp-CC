import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CalidadproductoprocesoPage } from './calidadproductoproceso.page';

describe('CalidadproductoprocesoPage', () => {
  let component: CalidadproductoprocesoPage;
  let fixture: ComponentFixture<CalidadproductoprocesoPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(CalidadproductoprocesoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
