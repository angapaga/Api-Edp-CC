import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CalidadproductoterminadoPage } from './calidadproductoterminado.page';

describe('CalidadproductoterminadoPage', () => {
  let component: CalidadproductoterminadoPage;
  let fixture: ComponentFixture<CalidadproductoterminadoPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(CalidadproductoterminadoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
