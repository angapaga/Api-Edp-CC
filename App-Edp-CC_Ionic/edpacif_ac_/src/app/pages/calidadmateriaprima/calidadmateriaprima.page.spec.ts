import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CalidadmateriaprimaPage } from './calidadmateriaprima.page';

describe('CalidadmateriaprimaPage', () => {
  let component: CalidadmateriaprimaPage;
  let fixture: ComponentFixture<CalidadmateriaprimaPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(CalidadmateriaprimaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
